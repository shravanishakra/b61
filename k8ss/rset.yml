apiVersion: apps/v1
kind: ReplicaSet
metadata:
   name: nginx-replicas
spec:
    replicas: 4
    selector:
      matchLabels:
         app: nginx
      matchExpressions:
      - {key: env, operator: In, values: [shraa, aru]}
    template:
      metadata:
        labels:
          app: shraa
          env: shraa
      spec:
        containers:
          - name: nginx-replicas
            image: nginx
            ports: 
             - containerPort: 80