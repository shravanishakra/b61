apiVersion: apps/v1
kind: ReplicaSet
metadata:
   name: nginx-replicas
spec:
    replicas: 4
    selector:
      matchLabels:
         app: nginx
    template:
      metadata:
        labels:
          app: nginx
      spec:
        containers:
          - name: nginx-replicas
            image: nginx
            ports: 
             - containerPort: 80