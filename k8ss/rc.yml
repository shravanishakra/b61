apiVersion: v1
kind:  ReplicationController
metadata:
   name: nginx-replicas
spec:
    replicas: 4
    selector:
      app: nginx
    template:
      metadata:
        labels:
          app: nginx
      spec:
        containers:
          - name: nginx-replicas
            image: nginx
            ports: 
              containerPort: 80